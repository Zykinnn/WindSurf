# Следующие шаги разработки HabitAI

## Что уже сделано ✅

1. ✅ Базовая структура проекта
2. ✅ package.json с зависимостями
3. ✅ Навигация (AppNavigator.jsx)
4. ✅ Тема и цветовая схема (theme/colors.js)
5. ✅ Константы (utils/constants.js)
6. ✅ OnboardingScreen с 3 слайдами
7. ✅ Заглушки экранов (Home, Chat, Progress, Settings)
8. ✅ Zustand stores (habitStore, settingsStore, chatStore)
9. ✅ Конфигурация (babel.config.js, app.json)

## Что нужно сделать дальше

### 1. Инициализация React Native проекта

Сейчас у нас есть структура, но нужно инициализировать настоящий React Native проект:

```bash
# Если хотите использовать Expo (проще)
npx create-expo-app HabitAI
# Затем скопируйте наши файлы в созданный проект

# Или React Native CLI (нативный подход)
npx react-native init HabitAI
# Затем скопируйте наши файлы
```

**Рекомендация:** Используйте Expo для быстрого старта.

### 2. Установить зависимости

```bash
cd HabitAI
npm install
```

### 3. Создать компоненты

#### VoiceOrTextInput.jsx
Компонент с переключением между голосовым и текстовым вводом:
- Голосовой режим: кнопка микрофона (hold to record)
- Текстовый режим: TextInput + кнопка отправки
- Интеграция с `@react-native-voice/voice`

Используйте **Промпт #2** из MASTER PROMPT.

#### HabitCard.jsx
Карточка привычки с gesture handlers:
- Свайп вверх → mark complete
- Свайп вниз → mark skipped
- Анимации с react-native-reanimated

Используйте **Промпт #5** из MASTER PROMPT.

#### AIMessage.jsx
Сообщение от AI с кнопкой озвучки:
- Bubble с текстом
- Кнопка "Послушать" (TTS)
- Auto-speak режим

### 4. Создать сервисы

#### grogAI.js
Сервис для работы с Grog API:
- `chat(userMessage, context)` - отправка сообщения
- `createHabitPlan(habitDescription)` - создание плана привычки
- `analyzeMissedDay(reason)` - анализ пропуска

Используйте **Промпт #3** из MASTER PROMPT.

#### supabase.js
Интеграция с Supabase:
- `createHabit(userId, habitData)`
- `markHabitComplete(habitId)`
- `markHabitSkipped(habitId, reason)`
- `getHabitLogs(habitId)`

Используйте **Промпт #4** из MASTER PROMPT.

#### tts.js
Text-to-Speech сервис:
- `speak(text, options)` - озвучка текста
- `stop()` - остановка
- `setSpeed(speed)` - скорость голоса

Используйте **Промпт #6** из MASTER PROMPT.

#### notifications.js
Push-уведомления:
- `scheduleDailyReminder(habit, time)`
- `scheduleEveningCheckIn(time)`

Используйте **Промпт #7** из MASTER PROMPT.

### 5. Доработать экраны

#### HomeScreen
- HabitCard с жестами
- Streak counter
- День X/66

#### ChatScreen
- История сообщений
- VoiceOrTextInput внизу
- Автоскролл к последнему сообщению

#### ProgressScreen
- Календарь (можно использовать react-native-calendars)
- Статистика (streak, success rate)

#### SettingsScreen
- Переключатели режимов
- Slider для скорости голоса
- Time picker для напоминаний

### 6. Настроить backend

#### Supabase
1. Создать проект на supabase.com
2. Создать таблицы (SQL из README.md)
3. Настроить RLS политики
4. Добавить credentials в .env

#### Grog API
1. Получить ключ на x.ai
2. Добавить в .env

#### Firebase
1. Создать проект в Firebase Console
2. Настроить Cloud Messaging
3. Скачать google-services.json (Android)
4. Скачать GoogleService-Info.plist (iOS)

### 7. Тестирование

```bash
# iOS
npm run ios

# Android
npm run android
```

### 8. Добавить анимации и polish

- Haptic feedback на действия
- Confetti при достижении streak milestone
- Smooth transitions
- Loading states

## Порядок разработки (рекомендуемый)

1. **Сначала UI без backend:**
   - Доработать OnboardingScreen (добавить VoiceOrTextInput)
   - Сделать полноценный HomeScreen с HabitCard
   - Добавить жесты

2. **Затем интеграция с Grog AI:**
   - Создать grogAI.js
   - Подключить в OnboardingScreen (создание привычки)
   - Подключить в ChatScreen

3. **Затем Supabase:**
   - Создать supabase.js
   - Сохранение привычек
   - Логирование действий

4. **Затем дополнительные фичи:**
   - TTS (озвучка)
   - Push-уведомления
   - Календарь прогресса

5. **Финальный polish:**
   - Анимации
   - Error handling
   - Offline mode
   - Testing

## Полезные команды

```bash
# Установка зависимостей
npm install

# Запуск на iOS
npm run ios

# Запуск на Android
npm run android

# Очистка кеша
npm start -- --reset-cache

# Установка pod для iOS
cd ios && pod install && cd ..
```

## Помощь

Если что-то непонятно:
1. Смотрите MASTER PROMPT с детальными инструкциями
2. Используйте специфичные промпты (#2-#10) для конкретных компонентов
3. Смотрите официальную документацию:
   - [React Native](https://reactnative.dev)
   - [Expo](https://docs.expo.dev)
   - [React Navigation](https://reactnavigation.org)
   - [Zustand](https://github.com/pmndrs/zustand)
   - [Supabase](https://supabase.com/docs)

## Готово к запуску!

Базовая структура создана. Следующий шаг — либо инициализировать React Native проект, либо начать создавать компоненты и сервисы.

Удачи! 🚀
